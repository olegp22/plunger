// models/Enums.cs  — single source of truth for game enums
// Place this file in the models/ folder.
// DELETE (or blank out) the old standalone Condition.cs and Cannon.cs.
namespace Plunger.Models
{
    public enum Condition { Run, Attached, Fall }
    public enum Cannon    { Plunger }
}
